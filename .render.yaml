services:
  - type: web
    name: faucet-automation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --worker-class gevent --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_ENV
        value: production
    autoDeploy: true
